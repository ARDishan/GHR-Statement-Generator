name: Test Production Runner

on:
  workflow_dispatch:

jobs:
  test:
    runs-on: self-hosted

    steps:
      - name: Check runner
        run: |
          echo "Runner is working!"
          echo "Hostname:"
          hostname
          echo "User:"
          whoami
          echo "Docker:"
          docker --version
          echo "Docker Compose:"
          docker compose version